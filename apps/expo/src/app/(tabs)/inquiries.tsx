import { SafeAreaView, Text } from "react-native";

export default function Inquiries() {
  return (
    <SafeAreaView
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        width: "100%",
        height: "100%",
      }}
    >
      <Text>Inquiries</Text>
    </SafeAreaView>
  );
}
