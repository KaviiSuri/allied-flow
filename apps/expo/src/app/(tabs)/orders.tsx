import { SafeAreaView, Text, View } from "react-native";


export default function Orders() {
  return (
    <SafeAreaView style={{
      display: 'flex',
      justifyContent: 'center',
      alignItems: 'center',
      width: '100%',
      height: '100%'
    }}>
      Orders
    </SafeAreaView>
  );
}
